# Makefile for building the 'school' executable

CC = gcc
CFLAGS = -Wall -Wextra

CFILES = main.c x.c y.c z.c

OBJECT = $(CFILES:.c=.o)

TARGET = school

all: $(TARGET)

$(TARGET): $(OBJECT)
	$(CC) $(CFLAGS) $^ -o $@

%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

clean:
	rm -f $(OBJECT) $(TARGET)
